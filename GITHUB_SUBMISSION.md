# GitHub Submission — Nexus AI

The final project has been prepared for:

https://github.com/kushagraacodes/NexusAI

## Final push

From the project root:

```bash
git init
git branch -M main
git remote add origin https://github.com/kushagraacodes/NexusAI.git
git add .
git commit -m "Final Nexus AI VITyarthi submission"
git push -u origin main
```

If a remote already exists, use:

```bash
git remote set-url origin https://github.com/kushagraacodes/NexusAI.git
git add .
git commit -m "Final Nexus AI VITyarthi submission"
git push -u origin main
```

Do **not** add a real `GEMINI_API_KEY` to the repository.

## Final verification
After pushing, open the repository and verify that `README.md`, `statement.md`, `src/`, `docs/diagrams/`, `docs/screenshots/`, `report/`, and `SUBMISSION_CHECKLIST.md` are visible.
